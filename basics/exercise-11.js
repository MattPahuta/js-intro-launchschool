// What value does the following expression evaluate to? 
parseInt('3.1415');
// 3 - parseInt takes the string, pulls the first numeric value, stops at the first non-numeric ('.') and ignores the rest;