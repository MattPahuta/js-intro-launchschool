// Will an error occur if you try to access an array element with an index thatis greater than or equal to the length of the aray?
let foo = ['a', 'b', 'c'];
console.log(foo.length); // 3
console.log(foo[3]); 

// Answer:
// Will return undefined since the array element at position 3 does not exist